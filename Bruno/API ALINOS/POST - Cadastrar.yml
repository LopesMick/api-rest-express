info:
  name: POST - Cadastrar
  type: http

http:
  method: POST
  url: http://localhost:3000/lista
  body:
    type: json
    data: |
      {
        "id": 5,
        "nome": "Pedro",
        "curso": "ADS"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
