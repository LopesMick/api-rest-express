info:
  name: GET - Buscar por ID
  type: http

http:
  method: GET
  url: http://localhost:3000/lista/1
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
